name: Basic Workflow Dispatch Example

on:
  workflow_dispatch:  # This allows the workflow to be manually triggered via the GitHub UI

jobs:
  deploy:
    runs-on: ubuntu-latest  # The runner will be an Ubuntu environment

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2  # Checkout the code from the repository

    - name: Run deployment script
      run: echo "We did the thing!"  # Placeholder for actual deployment or script
